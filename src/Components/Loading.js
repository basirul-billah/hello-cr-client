import React from 'react';

const Loading = () => {
    return (
        <div className='h-screen w-screen flex justify-center items-center '>
            <button className="btn btn-primary btn-square loading" />
        </div>
    );
};

export default Loading;